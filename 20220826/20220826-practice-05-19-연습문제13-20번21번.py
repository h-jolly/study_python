# 20번과 21번
# 20번
def test1(text): # 압축
    t_len = len(text)
    result = ''
    tmp_cnt = 0

    for i in range(t_len):
        cur_t = t_len[i]
        nex_t = t_len[i+1] if i == t_len else -1
        
        result


def test2(text): # 압축 풀기
    t_len = len(text)

    for i in range(t_len):

# 압축
src1 = 'aaaabbb'
src2 = 'aaaabccccaaaaacccfg'

# 압축 풀기


#

s_list = ['abc', 'bcd', 'abc', 'abba', 'cddc', 'opq', 'opq']
new_s_list = []

for i in range(len(s_list)):
    tmp = s_list[i]
    
    if tmp not in new_s_list:
        new_s_list.append(tmp)

print('정보 >', new_s_list)
